# PlatePoll 작업 요약

이번 세션에서 요청했던 작업들을 개선한 부분 / 막혔던 점 / 새롭게 배운 점으로 정리합니다.

## 개선한 부분

- **브랜딩 변경**: "오늘뭐먹지"(🍚) → "민주주의 정식"(🍽️)으로 홈/로그인·가입/네브바/브라우저 탭 메타데이터까지 전체 통일
- **홈 화면 비주얼 개편**: 장바구니 사진(basket.jpeg)을 히어로 배경으로 추가, 소개 문구 2줄 줄바꿈 정리
- **로그인·가입 화면**: basket 사진을 블러 처리한 배경 + 어두운 오버레이로 무드 있는 분위기 적용
- **색상 시스템 전면 개편**: 오렌지 단색 accent를 Burnt Sienna(포인트)/Ivory Sand(카드)/Stormy Sky(보조 정보)/Sage Green(1위·긍정 강조) 4색 팔레트로 교체, WCAG 대비 기준 맞춰 라이트/다크 값 조정. 이후 배경색만 화이트로 재조정
- **랭킹 화면 재설계**: 단순 리스트 → 1위는 큰 카드, 2위 이하는 동일 크기 그리드로 시각적 위계를 줌. 음식 사진이 없어 카테고리 기반 이모지로 대체
- **AI 점심 추천 봇 신규 추가**: Gemini API 연동, 식당 화면에 한 줄 무드 입력 → AI 추천 → 카카오맵 검색으로 실재 확인 → 등록까지 이어지는 흐름 구현
- **코드 재사용성 개선**: 식당 등록 로직(카카오 선택 → `addRestaurantAction` → 새로고침)을 `useAddRestaurant` 공유 훅으로 추출해 기존 등록 폼과 AI 추천 카드가 함께 사용
- **실제 회원가입 버그 발견 및 수정**: `cancelVoteAction`류가 아니라, 테스트 중 발견된 이메일 인증/로그인 관련 이슈를 진단하고 테스트 계정을 정리
- Vercel 프로덕션 배포 확인, `GEMINI_KEY` 환경변수 연동까지 마무리
- **회원가입 확인 메일이 프로덕션에서 `localhost`로 연결되는 치명적 버그 수정**: `SITE_URL`을 서버 전용 환경변수로 분리해 배포 방식과 무관하게 항상 올바른 주소가 반영되도록 개선, 이메일 발송 한도 초과 에러도 한글로 번역

## 막혔던 점

- **회원가입 이메일 인증 링크가 반복적으로 "만료됨" 오류를 표시**: Supabase 로그로 확인해보니 실제로는 인증이 이미 성공한 상태였음. 링크를 두 번째로 클릭했을 때만 발생하는 문제로, 메일 서비스의 보안 스캔이 링크를 미리 열어보면서 1회용 토큰을 먼저 소모했을 가능성이 큼 (재현성 있게 두 번 발생)
- **테스트 계정 비밀번호 분실**: 프로젝트에 비밀번호 재설정 기능이 없어(MVP 범위 제외 결정) DB에서 직접 재설정해야 했음
- **로컬 개발 서버 포트 충돌**: 다른 세션이 이미 3000번 포트를 점유하고 있어, 같은 프로젝트 폴더에서 새 서버를 띄우면 포트를 바꿔도 lock 충돌로 계속 실패. 브라우저 프리뷰 도구로 화면을 직접 확인하기 어려운 시간이 길었음
- **Gemini "Interactions API"(`ai.interactions.create`)의 에러 응답이 파싱 불가능**: 400 에러가 나도 실제 원인 메시지 없이 빈 메타데이터만 반환해서, 무엇이 잘못됐는지 한참 못 찾음
- **환경변수 이름 불일치**: 코드는 `GEMINI_API_KEY`를 읽는데 실제 Vercel에 등록된 이름은 `GEMINI_KEY`라 처음엔 서로 안 맞았음
- **모바일에서 회원가입 확인 메일 링크를 누르면 `localhost` 화면이 뜸**: 원인은 서버가 요청의 `Origin` 헤더가 없을 때 대비한 대체 값이 하드코딩된 `http://localhost:3000`이었던 것. `NEXT_PUBLIC_SITE_URL` 환경변수를 Vercel에 추가하고 재배포까지 했는데도 안 고쳐져서 한 번 더 막혔는데, 알고 보니 `NEXT_PUBLIC_` 접두어가 붙은 값은 "재배포"가 아니라 실제 "재빌드"를 해야만 새 값이 반영되는 구조라 원인이었음
- **Supabase 기본 이메일 발송 한도 초과**: 테스트하면서 회원가입을 반복하다 보니 Supabase의 시간당 이메일 발송 한도를 다 써서 이후 시도가 전부 "email rate limit exceeded"로 실패. 코드 문제가 아니라 서비스 자체의 제한이라 시간이 지나야 풀림

## 새롭게 배운 점

- Gemini의 신규 Interactions API는 아직 에러 핸들링이 불안정해서, 원인 진단이 중요한 상황에서는 검증된 구버전 `generateContent` API가 더 신뢰할 수 있었음
- 이메일 인증 링크는 메일 클라이언트/보안 게이트웨이의 자동 프리페치로 1회용 토큰이 미리 소모될 수 있어, 실제 사용자가 "정상 가입했는데도" 에러 화면을 보는 경우가 생길 수 있음 — 링크 대신 OTP 코드 입력 방식이 이런 문제에 더 안전한 대안
- Supabase 비밀번호는 `pgcrypto`의 `crypt()`/`gen_salt('bf')`로 관리자 권한 SQL을 통해 직접 재설정할 수 있음
- 같은 프로젝트 디렉터리에서 Next.js dev 서버를 두 번 띄우면, 포트가 다르더라도 디렉터리 단위 lock 때문에 두 번째 서버가 실패함
- AI가 검증 없이 자유롭게 추천하더라도, "실재 데이터를 검색해야만 실제 등록이 가능"하게 흐름을 설계하면 별도의 검증 로직 없이도 환각(hallucination)에 대한 실질적인 안전장치가 됨
- `NEXT_PUBLIC_` 접두어가 붙은 환경변수는 브라우저 노출용이라 **빌드 시점에 코드 안에 값이 그대로 박혀서**, 서버에서만 쓰는 값이라도 이 접두어를 붙이면 Vercel이 캐시된 빌드를 재사용하는 "재배포"만으로는 새 값이 반영되지 않을 수 있음 — 서버 전용 값은 접두어 없이 두면 매 요청마다 값을 새로 읽어서 이런 문제 자체가 없음
- Supabase의 기본 제공 이메일 발송 서비스는 "테스트용, best-effort"로 공식 문서에도 명시돼 있을 만큼 시간당 발송 한도가 낮음 — 실제 서비스 운영 전에는 Resend 등 커스텀 SMTP 연동이 필요함

---

# DB 스키마 및 핵심 기능 개발 세션 요약

이번 서비스에서 개선한 부분과 막혔던점, 새롭게 배운점을 정리합니다. (같은 프로젝트에서 진행된 또 다른 작업 세션 — 요구사항 정리부터 DB 설계, 인증, 투표 핵심 기능, UI 디자인까지)

## 요청 흐름 요약

1. 서비스 요구사항(회원가입, 투표 생성/참여, 랭킹 등)과 테이블 설계 초안을 주고 DB 스키마/RLS/라우트 구조 계획 요청
2. 계획서만 문서로 작성 요청 (개발은 보류)
3. 마이그레이션 실제 적용 요청
4. 개발 전 전체 작업을 TODO 리스트로 세분화 요청, 완료분 체크 요청
5. 섹션별로 순차적으로 개발 요청 (Supabase SSR 전환 → 인증 → 온보딩 → 공통 레이아웃 → 카카오맵 연동 → 식당 풀 → 투표 생성 → 투표 참여/결과 → 랭킹 → 보안 강화 → UI 디자인 폴리싱)
6. 매 섹션마다 commit/push 요청
7. 카카오 디벨로퍼스 앱 등록·API 활성화 등 외부 서비스 설정 진행하며 막히는 부분 질문
8. `.env.local` 보안(깃허브 업로드 방지) 확인 요청
9. UI 디자인 방향(컬러/폰트/애니메이션/로고)을 먼저 확인받고 진행하도록 요청

## 개선한 부분

- **DB 설계 확정 및 적용**: Organization/Department/Profile/Restaurant/Vote/VoteOption/VoteResponse 스키마와 RLS 정책을 확정하고, 마이그레이션 11개(스키마 8개 + 보안/성능 하드닝 3개)를 실제 Supabase 프로젝트에 적용
- **인증/온보딩 플로우**: 이메일·비밀번호 회원가입/로그인, 이메일 도메인 기반 조직 자동 매칭, 부서 선택/생성 온보딩 구현
- **핵심 기능 완성**: 카카오맵 검색 기반 식당 등록, 1·2·3지망 투표 생성/참여, 가중치(3/2/1점) 기반 득표 결과, 부서/회사/타 회사 랭킹 화면까지 전체 플로우 구현
- **보안 강화**: Server Action 전수 점검으로 실제 취약점(소유권 미검증) 발견 및 수정, 서로 다른 조직 테스트 계정으로 RLS 격리를 실제로 검증
- **디자인 시스템 도입**: 접근성 기준(WCAG 대비)을 만족하는 포인트 컬러, 한글 최적화 폰트, 로딩 스켈레톤, 트랜지션 적용
- **문서화**: 스키마/설계 문서(`lunch-vote-db-schema-plan.md`)와 진행 상황을 추적하는 TODO 체크리스트(`TODO.md`)를 계속 최신 상태로 유지

## ⭐ 카카오맵 연동 (강조)

이번 세션에서 가장 공들여 만들고, 동시에 가장 많이 삽질한 부분입니다. "카카오맵에서 식당을 검색해서 후보로 등록하고, 상세 화면에서 지도로 위치를 보여준다"는 기능을 처음부터 끝까지 붙였습니다.

**아키텍처 — 키를 2개로 분리**

카카오는 REST API 키와 JavaScript 키의 성격이 다릅니다. 이 차이를 그대로 설계에 반영했습니다.

| | REST API 키 | JavaScript 키 |
|---|---|---|
| 용도 | 키워드로 장소 검색 (서버에서 호출) | 지도 SDK 로드 (브라우저에서 직접 로드) |
| 노출 범위 | **서버 전용, 클라이언트에 절대 노출 안 됨** | 원래 공개되도록 설계됨 (카카오 콘솔에서 도메인 제한) |
| 코드 위치 | `KAKAO_REST_API_KEY` (`NEXT_PUBLIC_` 접두어 없음) | `NEXT_PUBLIC_KAKAO_JS_KEY` |

REST 키를 브라우저 코드에 넣으면 그대로 도용될 수 있어서, 검색은 반드시 우리 서버(Route Handler)를 한 번 거치도록 설계했습니다.

**구현한 것들**

- `src/lib/kakao.ts` — 서버 전용 키워드 검색 함수. 카카오 응답 필드(`place_name`, `x`/`y` 등)를 우리 스키마(`name`, `lat`/`lng` 등)로 매핑하고, 401(키 오류)/429(쿼터 초과) 등 상태별로 에러를 구분해 처리
- `src/app/api/kakao/search/route.ts` — 검색어를 받아 위 함수를 호출하는 Route Handler. 검색어 길이(2~50자) 서버측 검증 포함
- `src/components/kakao-search-box.tsx` — 디바운스(300ms) + `AbortController`로 이전 요청을 취소하는 검색창. 이미 등록된 식당은 "선택" 대신 "추가됨"으로 비활성화 표시
- `src/components/kakao-map-embed.tsx` — `next/script`로 Kakao Maps JS SDK를 로드해 위치에 마커를 찍고, "카카오맵에서 별점·리뷰 보기" 외부 링크도 함께 제공 (카카오는 별점 데이터를 API로 안 주기 때문에 링크로 대체)

**실제 겪은 문제와 해결 과정**

1. **카카오맵 API 미활성화 → 403**: 키는 정상인데 검색만 하면 403이 남. 카카오 서버 로그를 직접 조회해보니 `"App(PlatePoll) disabled OPEN_MAP_AND_LOCAL service."` — 카카오 디벨로퍼스에서 "제품 설정 > 카카오맵" 자체를 켜지 않았던 것. 활성화 후 바로 해결
2. **REST API 키 원문 노출 사고**: 키 형식을 확인하려고 curl로 직접 테스트하다가, 카카오 서버의 에러 메시지(`wrong appKey("...") format`)에 보낸 키가 그대로 echo되어 대화 로그에 노출됨. 카카오 콘솔의 "복제 키 생성 → 확인 → 확정" 무중단 키 교체 기능으로 즉시 재발급해서 대응
3. **한글 검색어 인코딩 문제**: Git Bash 환경에서 curl 커맨드라인에 한글 검색어("강남역 김밥")를 직접 넣으면 인코딩이 깨져서 결과가 0건으로 나옴. UTF-8 파일로 쿼리를 작성하는 Node 스크립트 방식으로 바꾸니 정상 동작 — 실제 앱은 브라우저 fetch로 쿼리를 보내니 이 문제와는 무관하다는 것도 함께 확인

**실제 검증**

브라우저에서 로그인 → 식당 화면 진입 → "스타벅스" 검색 → 실제 카카오 검색 결과 10건(주소·카테고리 포함) 확인 → 그중 하나를 실제로 추가 → "추가됨" 상태로 전환되고 상세 화면에서 지도까지 정상 렌더링되는 것을 끝까지 확인했습니다. 또한 프로덕션 빌드 결과물(`.next/static`)을 직접 grep해서 REST 키는 클라이언트 번들에 0건, 공개용 JS 키만 의도대로 포함된 것을 검증했습니다.

## 막혔던 점

- **dev 서버 포트 충돌**: 다른 세션에서 같은 프로젝트의 `next dev`가 포트 3000을 점유하고 있어서, 한동안 브라우저로 직접 동작을 확인하지 못하고 코드 검토·빌드로만 검증해야 했음
- **`.next` 캐시 손상**: dev 서버가 실행 중인 상태에서 캐시를 지우는 실수로 Turbopack 내부 상태가 깨져 모든 페이지가 404로 뜨는 문제 발생 (서버를 먼저 내리고 캐시를 지우는 순서로 해결)
- **카카오맵 API 미활성화**: 카카오 디벨로퍼스에서 "카카오맵" 제품을 활성화하지 않아 검색 API가 403 에러를 반환 — 제품 설정에서 활성화 후 해결 (자세한 진단 과정은 위 "카카오맵 연동" 참고)
- **환경변수 따옴표 문제**: `.env.local` 값들에 따옴표가 포함돼 있어 셸에서 직접 값을 꺼내 테스트할 때 파싱이 깨짐 (Next.js 자체 로더는 따옴표를 정상적으로 벗겨내므로 앱 동작에는 문제없었음)
- **수동 생성 테스트 계정 로그인 실패**: DB에 직접 삽입한 테스트 계정이 `confirmation_token` 등 토큰 컬럼이 `NULL`이라 GoTrue가 500 에러를 반환 — 빈 문자열로 채워서 해결
- **카카오 API 키 노출 사고**: 잘못된 키로 curl 테스트하다가 카카오 서버의 에러 메시지에 키 원문이 그대로 포함되어 대화 로그에 노출됨 — 키 재발급으로 대응

## 새롭게 배운 점

- **Next.js 16의 변경 사항**: `middleware.ts` → `proxy.ts`, `params`/`searchParams`가 Promise로 바뀐 점, Server Action은 UI를 우회해 직접 호출될 수 있으므로 액션 내부에서 항상 재검증이 필요하다는 점
- **Supabase RLS + `SECURITY DEFINER` 함수 조합**: 개인 데이터는 행 단위로 철저히 격리하면서, 집계 데이터(랭킹)만 조직 간에 공개하는 세밀한 권한 설계 방법
- **최신 ESLint 규칙의 실효성**: `react-hooks/set-state-in-effect`, `react-hooks/purity` 같은 규칙이 실제로 잠재적 버그(이펙트 내 동기 setState, 컴포넌트 순수성 위반)를 잡아준다는 것을 실감
- **접근성 대비 계산**: 감으로 색을 고르지 않고 WCAG 명도 대비 공식을 직접 계산해서 라이트/다크 모드 각각에 맞는 색상을 정하는 방법
- **Supabase Auth(GoTrue) 내부 요구사항**: `auth.users` 테이블에 직접 데이터를 넣을 때 토큰 컬럼들이 `NULL`이 아니라 빈 문자열이어야 한다는 점 등, 문서화되지 않은 스키마 제약
- **Tailwind v4의 `@theme` 토큰 시스템**: CSS 커스텀 프로퍼티 기반으로 라이트/다크 테마 색상을 한 곳에서 관리하는 방식
- **카카오 API 키의 이원 구조**: REST 키(서버 전용)와 JS 키(공개용, 도메인 제한)의 성격이 완전히 다르다는 것, 그리고 카카오 콘솔의 "복제 키 생성 → 확정" 방식이 서비스 중단 없이 노출된 키를 교체할 수 있는 실질적인 안전장치라는 것
